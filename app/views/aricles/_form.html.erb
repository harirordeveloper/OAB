<%= form_for(@aricle) do |f| %>
  <% if @aricle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aricle.errors.count, "error") %> prohibited this aricle from being saved:</h2>

      <ul>
      <% @aricle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
