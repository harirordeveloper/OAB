<%= form_for(@clinic_flow) do |f| %>
  <% if @clinic_flow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clinic_flow.errors.count, "error") %> prohibited this clinic_flow from being saved:</h2>

      <ul>
      <% @clinic_flow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :city %><br />
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :speciality %><br />
    <%= f.text_field :speciality %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
